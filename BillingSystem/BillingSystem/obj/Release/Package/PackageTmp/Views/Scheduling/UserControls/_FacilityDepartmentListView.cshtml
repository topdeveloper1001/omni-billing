@using BillingSystem.Model
@using Kendo.Mvc.UI
@model IEnumerable<FacilityStructure>

<style type="text/css">
    #TreeView .k-group li {
        float: left;
        width: 45%;
    }
    
</style>
@(Html.Kendo().TreeView().Name("treeviewFacilityDepartment").Checkboxes(checkboxes => checkboxes.Name("checkedFiles").CheckChildren(true)).Events(iten => iten.Check("onCheckDepartment")).HtmlAttributes(new { }).HighlightPath(true).Items(menu =>
      {
          var list = Model;
          foreach (var item in list)
          {
              menu.Add().Text(item.FacilityStructureName).HasChildren(false).Id(item.FacilityStructureId.ToString()).Checked(false).Expanded(false);
          }
      })
      )
@Html.Kendo().DeferredScripts()