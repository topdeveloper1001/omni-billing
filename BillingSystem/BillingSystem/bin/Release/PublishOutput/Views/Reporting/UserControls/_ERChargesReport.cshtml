@model List<BillingSystem.Model.CustomModel.ChargesReportCustomModel> 
@if (Model != null && Model.Any())
{
    var grid = new WebGrid(Model, ajaxUpdateContainerId: "gridContentERChargesReport", canPage: false, canSort: false);
    <div id="gridContentERChargesReport">
        @grid.GetHtml(htmlAttributes: new { id = "ERChargesReport" }, tableStyle: "table table_scroll_ER", headerStyle: "gridHead", footerStyle: "gridFooter",
            rowStyle: "gridRow", alternatingRowStyle: "gridRow", columns: grid.Columns(
                grid.Column("ActivityDate", "Activity Date", style: "col1",format: @<text>@if (item.ActivityDate != null)
                                                                                          {
                                                                                              @item.ActivityDate.ToString("dd/MM/yyyy")
                                                                                          }</text>),
                grid.Column("ActivityType", "Activity Type", style: "col2"),
                grid.Column("Payor", "Payor", style: "col2"),
                grid.Column("ActivityTotal", "Activity Cost", style: "col3"),
                grid.Column("Department", "Department A/C No.", style: "col5"),
                grid.Column("DepartmentName", "Department Description", style: "col5")
                ))
    </div>
}
else
{
    <h2>No Record found</h2>
}
<script>
    $(function() {
        if ($('#ERChargesReport tr').length > 0) {
            $('.table_scroll_ER').fixedHeaderTable({ cloneHeadToFoot: true, altClass: 'odd', autoShow: true });
            $("#ERChargesReport tbody tr:last").addClass('rowColor9');
        }
        SetGridSorting(SortERChargesReportGrid, "#gridContentERChargesReport");
    });
</script>