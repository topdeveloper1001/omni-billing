@using BillingSystem.Model.CustomModel

@model List<OpenOrderCustomModel>

@if (Model != null && Model.Count > 0)
{
    var grid = new WebGrid(Model, ajaxUpdateContainerId: "gridContentDischargeMedication", canPage: false, canSort: true);
    <div id="gridContentDischargeMedication">
        @grid.GetHtml(htmlAttributes: new { id = "DischargeMedicationGrid" }, tableStyle: "table gridscrollC", headerStyle: "gridHead", footerStyle: "gridFooter", rowStyle: "gridRow", alternatingRowStyle: "gridAltRow",
            columns:
                grid.Columns(
                    grid.Column("OrderCode", "Order Code", canSort: true, style: "col3"),
                    grid.Column("OrderDescription", "Order Description", canSort: true, style: "col4"),
                    grid.Column("CategoryName", "Order Category", canSort: true, style: "col6"),
                     grid.Column("SubCategoryName", "Order Sub-Category", canSort: true, style: "col7"),
                     grid.Column("Status", "Status", canSort: true, style: "col7"),
                     grid.Column("Quantity", "Quantity", canSort: true, style: "col2"),
                     grid.Column("FrequencyText", "Frequency", canSort: true, style: "col5"),
                     grid.Column("PeriodDays", "Duration", canSort: true, style: "col8"),
                     grid.Column("OrderNotes", "Comments", canSort: true, style: "col9")
                                              )
                                      )<h6>* Click on any column heading to sort</h6>
    </div>
}
else
{
    <h2>No Record found</h2>
}

<script>
    $(function () {
        //if ($('#colDischargeMedications').hasClass('in')) {
        //    $('.gridscrollC').fixedHeaderTable({ cloneHeadToFoot: true, altClass: 'odd', autoShow: true });
        //} else {
        //    $('#colDischargeMedications').addClass('in');
        //    $('.gridscrollC').fixedHeaderTable({ cloneHeadToFoot: true, altClass: 'odd', autoShow: true });
        //    $('#colDischargeMedications').removeClass('in');
        //}
        //SetGridSorting(BindDischargeMedicationBySort, "#gridContentDischargeMedication");
    });
</script>
